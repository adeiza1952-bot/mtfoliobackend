{
  "name": "mtfolio-backend",
  "version": "1.0.0",
  "description": "MTFolio backend API — Express server providing a health check and server-side/protected endpoints for the MTFolio frontend, backed by Firebase Admin SDK for token verification.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.3.1"
  }
}
